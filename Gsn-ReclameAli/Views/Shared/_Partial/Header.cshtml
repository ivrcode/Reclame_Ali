@using System.Security.Claims;
@using Gsn_ReclameAli.Common
@{
    var nomeCompleto = Context.User.GetNomeUsuario();
}
<header class="header navbar-area">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-12">
                <div class="nav-inner">
                    <!-- Start Navbar -->
                    <nav class="navbar navbar-expand-lg">

                        <button class="navbar-toggler mobile-menu-btn" type="button" data-bs-toggle="collapse"
                                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                                aria-expanded="false" aria-label="Toggle navigation">
                            <span class="toggler-icon"></span>
                            <span class="toggler-icon"></span>
                            <span class="toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse sub-menu-bar" id="navbarSupportedContent">
                            <ul id="nav" class="navbar-nav ms-auto">
                                <li class="nav-item">
                                    <a class="btn" href="@Url.Action("Index","Home")">Home</a>
                                </li>
                                <li class="nav-item">
                                    <a class="btn" href="@Url.Action("SobreNos","Home")">Sobre nós</a>
                                </li>

                                <li class="nav-item">
                                    <a class="btn" href="@Url.Action("ListarProblema","Problema")">Listar Problemas</a>
                                </li>
                                @if (string.IsNullOrEmpty(nomeCompleto))
                                {

                                }
                                else
                                {
                                    <li class="nav-item">
                                        <a class="btn" href="@Url.Action("ListarUsuario","Usuario")">Listar Usuario</a>
                                    </li>
                                }
                            </ul>
                        </div> <!-- navbar collapse -->
                        @if (string.IsNullOrEmpty(nomeCompleto))
                        {
                            <div class="button">
                                <a class="btn" href="@Url.Action("Index","Usuario")">Login</a>
                            </div>
                        }
                        else
                        {
                            <div class="badge badge-pill badge-light">
                                <a class="btn" href="@Url.Action("Index","Usuario")">Sair</a>
                                <span>@nomeCompleto</span>
                            </div>
                        }
                    </nav>
                    <!-- End Navbar -->
                </div>
            </div>
        </div> <!-- row -->
    </div>
</header>